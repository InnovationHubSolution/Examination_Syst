import React from 'react';
import { Container, Typography } from '@mui/material';

const Profile = () => {
    return (
        <Container>
            <Typography variant="h4">Profile</Typography>
            <Typography>Profile page - Update your personal information</Typography>
        </Container>
    );
};

export default Profile;
