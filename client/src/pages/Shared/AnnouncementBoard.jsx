import React from 'react';
import { Container, Typography } from '@mui/material';

export default () => (
    <Container>
        <Typography variant="h4">Announcement Board</Typography>
    </Container>
);
